#Generic shortcuts:
alias music="ncmpcpp"
alias clock="ncmpcpp -s clock"
alias visualizer="ncmpcpp -s visualizer"
alias news="newsbeuter"
alias email="neomutt"
alias files="ranger"
alias chat="weechat"
alias audio="ncpamixer"
alias calender="calcurse"
alias getmail="offlineimap && notmuch new"
alias gm="offlineimap && notmuch new"

# Filesystem
alias ..='cd ..'            # Go up one directory
alias ...='cd ../..'        # Go up two directories
alias ....='cd ../../..'    # And for good measure
alias ls='ls --color=auto --group-directories-first'  # gimmie colors
alias l='ls -lah --color=auto --group-directories-first'   # Long view, show hidden
alias la='ls -AF --color=auto --group-directories-first'   # Compact view, show hidden
alias ll='ls -lFh --color=auto --group-directories-first'  # Long view, no hidden

# Tmux alias for color for TERM
#alias tmux='TERM=xterm-256color \tmux'

# Helpers
alias grep='grep --color=auto' # Always highlight grep search term
alias ping='ping -c 5'         # Pings with 5 packets, not unlimited
alias df='df -h'               # Disk free, in gigabytes, not bytes
alias du='du -sh'            # Calculate total disk usage for a folder
alias svim="sudo vim" 	       # Run vim as super user
alias Root='su -'
alias duu='for t in files links directories; do echo `find . -maxdepth 1 -type ${t:0:1} | wc -l` $t; done 2> /dev/null'

# Arbit aliases
alias e='vim'
alias Z='clear'
alias V='cd ~'
alias cd.='cd ..'
alias cd..='cd ..'

#Use neovim instead of vim
alias vim='nvim'

#pacaur/pacman
alias progs='pacman -Qet' #List Programs installed
alias orphs='pacman -Qdt' #List prphan Programs
alias sp='sudo pacman'

#youtube-dl
alias Y='youtube-dl -c -o "%(autonumber)03d-%(title)s.%(ext)s"'
alias Ys='youtube-dl -c -o "%(title)s.%(ext)s" --no-playlist'

# curl for useragents
alias iecurl="curl -H \"User-Agent: Mozilla/5.0 (Windows; U; MSIE 6.0; Windows NT 5.1; SV1; .NET CLR 2.0.50727)\""
alias ffcurl="curl -H \"User-Agent: Mozilla/5.0 (Windows; U; Windows NT 5.1; en-US; rv:1.9.0.8) Gecko/2009032609 Firefox/3.0.0 (.NET CLR 3.5.30729)\""

# HG ALIASES
# alias hgst='hg status'
# alias hgd='hg diff | $GIT_EDITOR -'
# alias hgo='hg outgoing'

# aliases for Tmux
# alias tmux='tmux -2'
# alias ta='tmux attach -t'
# alias tnew='tmux new -s'
# alias tls='tmux ls'
# alias tkill='tmux kill-session -t'
